'use client';
import { Carousel, Description, Follow } from '@/components';

export default function Home() {
    return (
        <main>
            <Carousel />
            <Description />
            <Follow />
        </main>
    );
};