'use client';
import { Carousel, Description } from '@/components';

export default function Home() {
    return (
        <main>
            <Carousel />
            <Description />

        </main>
    );
};