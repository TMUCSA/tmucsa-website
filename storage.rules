rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if true;
      // All uploads and deletions pass through a session-protected Next.js API
      // using Firebase Admin. Browser clients never receive Storage write access.
      allow write: if false;
    }
  }
}
